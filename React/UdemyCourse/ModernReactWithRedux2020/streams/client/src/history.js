import { createBrowserHistory } from 'history'; 
export default createBrowserHistory();


// This file create a oun history object and export it. The reason for this is we will use this object to redirect the user for some page after complite something.
// For example, in this project, we create this because we need redirect the user for main page after he finshed the filling the form to Create Stream.  